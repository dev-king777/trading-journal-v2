'use client';

import AppLayout from '@/components/layout/AppLayout';
import TradesListContent from '@/components/trades/TradesListContent';

export default function TradesPage() {
  return (
    <AppLayout>
      <TradesListContent />
    </AppLayout>
  );
}
